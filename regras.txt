PROGRAM → program id begin DECL BLOCK end
DECL → VAR DECL
DECL → PROC DECL
DECL → REC DECL
DECL → ENUM DECL
VAR DECL → var id ”:”TYPE VAR DECL2
VAR DECL → var id := EXP
VAR DECL2 → := EXP
VAR DECL2 → ε
PROC DECL → procedure id ( PARAMS ) PDT begin PD2 STMT LIST end
REC DECL → struct id { PARAMS }
PARAMS → PF DECL PD
PARAMS → ε
PD → ”,”PF DECL PD
PD → ”;”PF DECL PD
PD → ε
PDT → ”:”TYPE
PDT → ε
PD2 → DECL BLOCK in
PD2 → ε
DECL BLOCK → DECL DECL BLOCK2
DECL BLOCK → ε
DECL BLOCK2 → ”;”DECL DECL BLOCK2
DECL BLOCK2 → ε
ENUM DECL → id ”=”{ id ENUM FIELD } of TYPE
ENUM FIELD → ”,”id ENUM FIELD
ENUM FIELD → ε
PF DECL → id ”:”TYPE
STMT LIST → STMT STMT LIST2
STMT LIST → ε
STMT LIST2 → ”;”STMT STMT LIST2
STMT LIST2 → ε
EXP → EXP LOG OP EXP
EXP → not EXP
EXP → EXP REL OP EXP
EXP → EXP ARITH OP EXP
EXP → LITERAL
EXP → CALL STMT
EXP → new NAME
EXP → VAR
EXP → REF VAR
EXP → DEREF VAR
EXP → ( EXP )
REF VAR → ref ( VAR )
DEREF VAR → deref ( VAR )
DEREF VAR → deref ( DEREF VAR )
VAR → NAME
VAR → EXP ”.”NAME
LOG OP → &&
LOG OP →||
REL OP →<
REL OP →<=
REL OP →>
REL OP →>=
REL OP →=
REL OP →<>
ARITH OP → +
ARITH OP → −
ARITH OP → ∗
ARITH OP → /
ARITH OP → ˆ
LITERAL → FLOAT LITERAL
LITERAL → INT LITERAL
LITERAL → STRING LITERAL
LITERAL → BOOL LITERAL
LITERAL → null
BOOL LITERAL → true
BOOL LITERAL → false
STMT → ASSIGN STMT
STMT → IF STMT
STMT → WHILE STMT
STMT → RETURN STMT
STMT → CALL STMT
ASSIGN STMT → VAR ”:=”EXP
ASSIGN STMT → DEREF VAR ”:=”EXP
IF STMT → if EXP then STMT LIST IF STMT2 fi
IF STMT2 → else STMT LIST
IF STMT2 → ε
WHILE STMT → while EXP do STMT LIST od
RETURN STMT → return RETURN STMT2
RETURN STMT2 → EXP
RETURN STMT2 → ε
CALL STMT → NAME ( CALL ARGS )
CALL ARGS → EXP CALL ARGS2
CALL ARGS → ε
CALL ARGS2 → ”,”EXP CALL ARGS2
CALL ARGS2 → ε
TYPE → float
TYPE → int
TYPE → string
TYPE → bool
TYPE → NAME
TYPE → ref ( TYPE )