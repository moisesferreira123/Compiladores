#ifndef TOKENS_HPP
#define TOKENS_HPP

enum Tokens {
   TOKEN_INT = 360,
   TOKEN_FLOAT,
   TOKEN_STRING,
   TOKEN_BOOL,
   TOKEN_PROGRAM,
   TOKEN_PROCEDURE,
   TOKEN_BEGIN,
   TOKEN_END,
   TOKEN_VAR,
   TOKEN_IN,
   TOKEN_STRUCT,
   TOKEN_NOT,
   TOKEN_NULL,
   TOKEN_NEW,
   TOKEN_REF,
   TOKEN_DEREF,
   TOKEN_TRUE,
   TOKEN_FALSE,
   TOKEN_IF,
   TOKEN_THEN,
   TOKEN_ELSE,
   TOKEN_FI,
   TOKEN_WHILE,
   TOKEN_DO,
   TOKEN_OD,
   TOKEN_RETURN,
   TOKEN_ENUM,
   TOKEN_OF,
   TOKEN_AND,
   TOKEN_OR,
   TOKEN_COMP,
   TOKEN_ADD,
   TOKEN_SUB,
   TOKEN_MULT,
   TOKEN_DIV,
   TOKEN_POT,
   TOKEN_ATTRIBUTION,
   TOKEN_COLON,
   TOKEN_OPEN_PARENTHESIS,
   TOKEN_CLOSE_PARENTHESIS,
   TOKEN_SEMICOLON,
   TOKEN_COMMA,
   TOKEN_DOT,
   TOKEN_OPEN_BRACES,
   TOKEN_CLOSE_BRACES,
   TOKEN_SINGLE_LINE_COMMENT,
   TOKEN_MULTIPLE_LINE_COMMENT,
   NAME,
   INT_LITERAL,
   FLOAT_LITERAL,
   STRING_LITERAL,
   TOKEN_ERRO
};

#endif // TOKENS_HPP
