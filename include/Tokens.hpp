#ifndef TOKENS_HPP
#define TOKENS_HPP

enum Tokens {
   FIRST_TOKEN,
   TOKEN_INT,
   TOKEN_FLOAT,
   TOKEN_STRING,
   TOKEN_BOOL,
   TOKEN_PROGRAM,
   TOKEN_PROCEDURE,
   TOKEN_BEGIN,
   TOKEN_END,
   TOKEN_VAR,
   TOKEN_IN,
   TOKEN_STRUCT,
   TOKEN_NOT,
   TOKEN_NULL,
   TOKEN_NEW,
   TOKEN_REF,
   TOKEN_DEREF,
   TOKEN_TRUE,
   TOKEN_FALSE,
   TOKEN_IF,
   TOKEN_THEN,
   TOKEN_ELSE,
   TOKEN_FI,
   TOKEN_WHILE,
   TOKEN_DO,
   TOKEN_OD,
   TOKEN_RETURN,
   TOKEN_ENUM,
   TOKEN_OF,
   TOKEN_AND,
   TOKEN_OR,
   TOKEN_COMP,
   TOKEN_ADD,
   TOKEN_SUB,
   TOKEN_MULT,
   TOKEN_DIV,
   TOKEN_POT,
   TOKEN_ATTRIBUTION,
   TOKEN_COLON,
   TOKEN_OPEN_PARENTHESIS,
   TOKEN_CLOSE_PARENTHESIS,
   TOKEN_SEMICOLON,
   TOKEN_COMMA,
   TOKEN_DOT,
   TOKEN_OPEN_BRACES,
   TOKEN_CLOSE_BRACES,
   TOKEN_SINGLE_LINE_COMMENT,
   TOKEN_MULTIPLE_LINE_COMMENT,
   NAME,
   INT_LITERAL,
   FLOAT_LITERAL,
   STRING_LITERAL,
   TOKEN_ERRO,
   TOKEN_EQUAL,
   TOKEN_CIPHER, // CIFRÃO
   TOKEN_EPSILON,
   LAST_TOKEN,
};

enum NonTerminalTokens {
   FIRST_TOKEN_NT = 360,
   PROGRAM,
   PROGRAM_DECL,
   PROGRAM_DECL_,
   DECL,
   VAR_DECL,
   VAR_DECL_,
   VAR_EXP_DECL,
   PROC_DECL,
   PROC_PARAMFIELD_DECL,
   PROC_PARAMFIELD_DECL_,
   PROC_TYPE_DECL,
   PROC_DECL_DECL,
   REC_DECL,
   REC_PARAMFIELD_DECL,
   REC_PARAMFIELD_DECL_,
   ENUM_DECL,
   ENUM_DECL_,
   PARAMFIELD_DECL,
   STMT_LIST,
   STMT_LIST_,
   EXP,
   OR_OP_,
   AND_OP,
   AND_OP_,
   NOT_OP,
   NOT_OP_,
   EXP_REL,
   EXP_REL_,
   EXP_ARITH,
   EXP_ARITH_,
   TERM,
   TERM_,
   POW,
   POW_,
   VAR,
   VAR_,
   FACT,
   CALL_STMT,
   CALL_ARGS,
   CALL_ARGS_,
   REF_VAR,
   DEREF_VAR,
   DEREF_VAR_,
   LITERAL,
   BOOL_LITERAL,
   REL_OP,
   VAR2,
   VAR2_,
   VAR2__,
   EXP2,
   FACT2,
   EXP3,
   OR_OP3,
   AND_OP3,
   EXP_REL3,
   EXP_ARITH3,
   TERM3,
   POW3,
   FACT3,
   EXP4,
   OR_OP4_,
   AND_OP4,
   AND_OP4_,
   NOT_OP4,
   NOT_OP4_,
   EXP_REL4,
   EXP_REL4_,
   EXP_ARITH4,
   EXP_ARITH4_,
   TERM4,
   TERM4_,
   POW4,
   POW4_,
   FACT4,
   STMT,
   STMT_,
   STMT_DEREF_,
   ASSIGN_STMT,
   EXP5,
   FACT5,
   IF_STMT,
   ELSE_STMT,
   WHILE_STMT,
   RETURN_STMT,
   TYPE,
   LAST_TOKEN_NT
};

#endif // TOKENS_HPP
