program personexample
begin
   struct {
      name: string;
      age: int
   } Person;
   procedure main()
   begin
      var person: Person
   in
      person.age := 2025 - 2003;
      if person.age > 18 then
         person.name := "Pedro"
      fi
   end
end